#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

puts "Welcome to Tic Tac Toe!"
puts "Where would you like to go?"

return board = Array.new(9, " ")

input = gets.strip
index = input_to_index(input)
move(board, index, "X")
return display_board(move)
